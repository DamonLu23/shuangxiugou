# PROJECT.md — 双休购

> 最后更新：2026-09-18
> 维护规范：每次数据周更或阶段完成后更新「当前状态 / 待办 / 下一步」；
> 操作流程见 `AGENTS.md` 与 skill `data-weekly-refresh`。

## 1. 项目目标

- **一句话**：只推荐严格执行双休（L1/L2）的企业及其在招岗位、代表商品。
- **产品形态**：本地静态版（零服务器，双击即用）／Vue3 SPA（可选自托管后端）／开源数据包（周更）。
- **底线承诺**：面向用户只展示白名单，永不展示负面评级；L3~L6 只存在本地数据层。
- **阶段目标**：
  1. 覆盖率：企业池 1000+（已达成），白名单持续增长
  2. 准确度：多源交叉验证 + 证据可复核 + 企业 48h 申诉
  3. 可持续：采集/导出/发布全自动化（CI 周更）
- **非目标（当前）**：不做返佣/联盟变现、不做 App（iOS 已弃用）、不做用户账户体系。

## 2. 当前状态（2026-09-18）

| 维度 | 状态 |
|---|---|
| 企业池 | **1019 家**（seed.csv；榜单扩编：财富中国500强/中企联500强/互联网百强，295→1019） |
| 白名单（L1/L2） | **19 家**（L1 11 + L2 8；本轮新增 兴业银行/燕京啤酒/科大讯飞/天津友发，满帮/贝壳来自抽样） |
| 待验证 | 953 家 L6；有证据企业 66 家 |
| 岗位 | **370 个在招**（jobs.json，09-18 刷新；过期岗位已按成功来源老化） |
| 商品 | goods.csv 15 条；goods_candidates.csv 120 条（苏宁候选，待人工确认补链接） |
| 证据 | DB 共 297 条：job_post 173 / review 59 / review_promo 65（智联 + Bing + UGC） |
| 测试 | crawler 87 / backend 53 / web 5，全过；`verify.py` ALL PASS（企业 1019 / L1-L2 交叉验证 19 家） |
| CI | 周更：分片 1/4 + 白名单回灌 + 全采白名单；手动可 `full` 全量 |
| 本地服务 | 已关闭（按需启动后端 8000 / 前端 5173，见 AGENTS.md） |
| 已推送提交 | `81776f4` 榜单扩编+小红书+苏宁候选 → `c582ddc` AGENTS/skill → `592ca31` CI回灌+岗位老化修复 → `7142b59` PROJECT.md |
| 技能 | `data-weekly-refresh` skill 已提交；**重启 opencode 后生效** |
| 采集进度 | 分片 1/4 完成：274 家、新增 75 条证据（Bing 命中 82 条 / 智联 38 条）；chunk 2/4~4/4 待跑 |

## 3. 关键决策

| 决策 | 结论 | 依据/记录 |
|---|---|---|
| 数据发布策略 | 白名单模式 + 保守策略：只发布 L1/L2；`--full` 全量只进 `data/local/` | 降低名誉风险；见 README/AGENTS.md |
| 数据源取舍 | 启用：智联、Bing 片段、UGC、官网框架；弃用：BOSS/51job/猎聘/看准/LinkedIn | 均有实测记录，见 `docs/sources.md` |
| 小红书 | 不直抓（签名风控），经 Bing `site:xiaohongshu.com` 间接获取，命中按口碑 0.9 计权 | Phase 3 |
| 商品候选源 | 京东不可用（登录墙+SPA）→ 改用苏宁静态页；**只抓信息，不存链接与价格** | Phase 2，与用户约定 |
| 企业池扩编 | 榜单名录化（fetch_lists → seed_extra → expand_seed → import_seed），幂等去重 | 295→1019 |
| CI 周更机制 | 按 key 哈希分片 1/4 + 从 `companies.json` 回灌白名单证据 + 白名单每轮全采；聚合以 DB 全量证据为准 | 修复分片导致的白名单震荡 |
| 等级全量重算 | 数据变更后按 DB 全量证据重算全部企业（本轮 recalc 1019 家）；避免历史局部聚合造成等级漂移 | 分片采集与聚合一致性 |
| 白名单动态性 | 证据变化会进/出白名单：本轮 顺丰（新负向口碑）移出，海天升级 L1、燕京/兴业等 6 家新进 | 白名单 = 当前证据结论，非永久名单 |
| 岗位老化 | 仅对本轮**成功采集**的来源、按企业老化；失败不误删 | 修复原全局预老化风险 |
| 反馈通道 | 本地版维持跳转 GitHub Issue；在线版无感通道（表单→后台建 Issue）**暂缓** | 用户决策（Phase 4 推迟） |
| 淘宝联盟 API | 免费但需 ICP 备案（域名+10 页面），暂不接入；资质办下来后再评估 | 成本≈0，门槛是备案 |
| 服务部署 | 无生产部署；M0 资质（域名/服务器/ICP/Apple 开发者）全部未启动 | 在线功能无真实用户 |

## 4. 待办

**进行中**
- [ ] 无（分片 1/4 + 岗位刷新 + 导出一轮已完成，等待下一轮）

**优先级高**
- [ ] 重启 opencode，验证 `data-weekly-refresh` skill 可加载
- [ ] 跑剩余分片（chunk 2/4、3/4、4/4，每条 ~2h）或依赖 CI 周更自然覆盖
- [ ] goods_candidates 人工确认：挑 3~5 家白名单企业，补 `official_link` 入 goods.csv
- [ ] 对移出白名单的企业（顺丰等）人工抽检证据，确认非误判

**优先级中**
- [ ] 官网岗位源逐家适配（当前 14 家全部 `enabled=false`；ATS 公开接口优先）
- [ ] 榜单长 key 别名优化（如「中芯国际集成电路制造」→ 中芯国际，提高口碑命中率）
- [ ] UGC 通道运营（当前空转，是权重最高的证据来源）

**暂缓/未启动**
- [ ] Phase 4 无感反馈通道（网页表单 → 后台自动建 Issue）
- [ ] M0 资质：域名/服务器/ICP 备案/淘宝联盟 AppKey/Apple 开发者
- [ ] alembic 迁移、crawler/backend 单一 venv（技术债）

## 5. 风险

| 风险 | 影响 | 缓解 |
|---|---|---|
| Bing 自动化诱饵（间歇性） | 口碑证据 0 命中，白名单扩张慢 | 本轮实测 274 家命中 82 条，诱饵为间歇性；解析层安全丢弃，不影响数据质量 |
| 智联页面偶发卡死 | 采集进程无限挂起（已实测：一次卡死 >30 分钟） | 已修复：`page.evaluate` 加 30s 超时（`zhaopin.py`），下次采集生效 |
| 智联风控/改版（单源依赖） | 岗位与证据采集中断 | 限速、错误隔离、健康报告；官网/UGC 作为备份通道 |
| 电商反爬变化（京东已失效） | 商品候选断供 | 苏宁现状；淘宝联盟（备案后）作为官方替代 |
| 覆盖率不足（953 家 L6） | 用户「搜不到」，体验差 | 分片周更持续覆盖；优先收录用户关心的品牌 |
| 采集长任务（全量 8h+） | 本地/CI 超时 | `--chunk` 分片 + `--stale-days` 增量；后台 + 日志 |
| 企业名誉/合规 | 申诉/投诉 | 白名单承诺 + 证据链接可复核 + 48h 申诉通道 + 免责声明 |
| 单人维护、无部署 | 项目停更风险；在线功能无用户 | 文档/skill 沉淀流程，降低接手成本 |
| 数据时效 | 岗位/商品过期 | 岗位老化 + 周更 + `job_post` 低权重（0.4） |

## 6. 下一步

1. **今天**：推送本轮结果（19 家白名单 / 370 岗位 / bundle / PROJECT.md）；重启 opencode 验证 skill。
2. **本周**：跑 chunk 2/4（后台 ~2h）或触发 CI 手动全量；goods 候选人工确认 3~5 家并入库。
3. **近期**：复核移出白名单企业证据（顺丰）；白名单覆盖率报告（按行业/城市）；UGC 通道小规模启用。
4. **中期**：官网 ATS 适配攻坚（Moka/北森/大易）；长 key 别名表。
5. **里程碑**：白名单 ≥50 家且证据交叉验证达标后，再评估 M0 资质与线上部署。
